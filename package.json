{
  "name": "app",
  "private": "true",
  "scripts": {
    "build": "echo 'nothing to build' && exit 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
