{
  "name": "app",
  "private": "true",
  "scripts": {
    "build": "echo 'nothing to build' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.6"
  }
}
