<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Coffee Log<%= " - #{content_for(:title)}" if content_for?(:title) %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous" data-turbo-track="reload">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.6.1/font/bootstrap-icons.css" data-turbo-track="reload">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track' => 'reload' %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous" data-turbo-track="reload"></script>

    <%= javascript_importmap_tags %>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= yield :head %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="<%= root_path %>">Coffee Log</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%- viewing_own_log = request.path == log_entries_path(current_user.log) -%>
              <a class="nav-link<%= ' active' if viewing_own_log %>" <%= 'aria-current="page"' if viewing_own_log %> href="<%= log_entries_path(current_user.log) %>">
                My Log
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= coffees_path %>">Coffees</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= coffee_brands_path %>">Brands</a>
            </li>
            <li class="nav-item dropdown">
              <%- viewing_other_log = !viewing_own_log && request.path.starts_with?(logs_path) -%>
              <a class="nav-link dropdown-toggle<%= ' active' if viewing_other_log %>" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Other Logs
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <% @logs.each do |log| %>
                  <%- next if log == current_user.log -%>
                  <li>
                    <a class="dropdown-item<%= ' active' if request.path == log_entries_path(log) %>" href="<%= log_entries_path(log) %>">
                      <%= log.title %>
                    </a>
                  </li>
                <% end %>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <div id="flash">
        <% flash.each do |key, value| %>
          <div class="alert <%= flash_alert_class(key) %>">
            <%= flash_icon_tag(key) %>
            <%= value %>
          </div>
        <% end %>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
