<%= turbo_stream.update "selected-coffee-card" do %>
  <%= render "selected_coffee",
             coffee: @coffee,
             back_url: log_entries_path(current_user.log, coffee_id: @coffee.id, query: params[:query])
  %>
<% end %>

<%= turbo_stream.remove "#{dom_id(@coffee)}-search-result" %>
